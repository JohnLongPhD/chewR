% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autocrop.R
\name{gum_autocrop_file}
\alias{gum_autocrop_file}
\title{Auto-crop gum wafer from a scan (EBImage backend)}
\usage{
gum_autocrop_file(
  infile,
  outfile = NULL,
  sat_floor = 0.18,
  min_pixels = 1200,
  pad_frac = 0.1,
  cap_highlights = TRUE,
  highlight_cap = 0.98,
  save_debug = FALSE
)
}
\arguments{
\item{infile}{Path to input image (png/jpg/jpeg)}

\item{outfile}{Path to write the cropped image. If NULL, a temp file is used.}

\item{sat_floor}{Minimum saturation threshold floor (0–1)}

\item{min_pixels}{Minimum connected component area in pixels}

\item{pad_frac}{Padding fraction of bbox size (0–1)}

\item{cap_highlights}{If TRUE, exclude very bright pixels (glare)}

\item{highlight_cap}{V (value) cutoff to exclude highlights (0–1)}

\item{save_debug}{If TRUE, writes a mask overlay image (outfile*_mask.png)}
}
\value{
Path to the cropped image (invisible).
}
\description{
Uses saturation + optional highlight capping, selects the largest component,
pads the bounding box, and writes a cropped image (PNG by default).
}
\keyword{internal}
